{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "classStructure": {
      ".write": "auth != null"
    },
    "piketSchedule": {
      ".write": "auth != null"
    },
    "lessonSchedule": {
      ".write": "auth != null"
    },
    "assignments": {
      ".write": "auth != null",
      "$assignmentId": {
        ".validate": "newData.hasChildren(['title', 'subject', 'dueDate', 'createdAt', 'createdBy'])"
      }
    }
  }
}
